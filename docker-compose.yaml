version: "3"

services:
  backend:
    build: ./back-end
    restart: always
    working_dir: /app
    volumes:
      - ./back-end/src:/app
      - ./back-end/utils:/utils
      - ./resources:/data
    command: ["/utils/wait-for-postgres.sh", "db", "python3.8", "san11_platform_server.py"]
    depends_on:
      - db
      - envoy

  nginx:
    image: nginx:latest
    volumes: 
      - ./resources:/data
      - ./back-end/nginx/conf:/etc/nginx/conf.d
    command: []
    ports:
      - "8080:8080"

  envoy:
    image: envoyproxy/envoy-alpine:v1.13.0
    restart: always
    volumes:
      - ./back-end/envoy:/etc/envoy
    command: /usr/local/bin/envoy -c /etc/envoy/envoy.yaml -l trace --log-path /tmp/envoy_info.log
    ports:
      - "8090:8090"

  db:
    build: ./database
    restart: always
    volumes: 
      - ./database/postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  frontend:
    build: ./front-end
    working_dir: /app
    volumes:
      - ./front-end:/app
    command: bash -c "npm install && ng serve --prod --host 0.0.0.0 --disable-host-check"
    depends_on: 
      - backend
      - nginx
    ports:
      - "80:4200"
    
